# Memory Index

- [Project Context](project-context.md) — Architecture state, constraints, active tech debt
- Gridix 2026-06-19 — Workbench refactor completed through Phase 7 RightInspector, then pivoted to an April-style stable shell plus Dockable Workbench v2. Current boundary: TopBar is the only global launcher; PrimarySidebar is a stable 4月式 navigation/content region controlled by `ToggleSidebar`; the default dock workspace now matches the user-requested screenshot model: Query Results/data workspace in the center, SQL editor bottom, ER right, not Explorer or Inspector. The user-approved default screenshot ratio is canonical: fixed PrimarySidebar `280px`, query/ER `0.73/0.27`, results/editor `0.69/0.31`, explicit left dock retain `0.79`. Explorer/Filters/Objects and RightInspector still have `WorkbenchSurfaceKind` descriptors and can be explicitly revealed as dock surfaces, but ordinary sidebar show/hide must not mutate the dock tree. ActivityBar/SurfaceRail is dormant. Next slice: split fixed sidebar navigation state from docked navigation surfaces and migrate remaining fixed-region chrome into shared surface shell controls without reintroducing duplicate launchers.
- Gridix 2026-06-20 — ER diagram visual redesign implemented in `src/ui/components/er_diagram/render.rs`: schema-canvas background, framed toolbar, database object cards with semantic accent rail, PK/FK badges, key-row highlighting, monospace type alignment, relationship halo/endpoints/cardinality pills, and themed empty/loading cards. Preserve this design language; do not revert to plain boxes plus thin lines.
- Gridix 2026-06-20 — ER diagram logic audit fixed egui drag semantics: table dragging and canvas panning must use per-frame pointer deltas stored in `ERDiagramState`, scaled by current zoom. Click selection must not start a drag, and overlapping table hit-testing should prefer the last-drawn/topmost table.
- Gridix 2026-06-21 — Dialog/workspace utility surfaces are part of the UI system, not throwaway egui windows. Keybindings, Help/Learning, toolbar action menu, and theme selector should share `DialogContent`, `WorkspaceDialogShell`, and `PickerDialogShell`: clean modal frame, restrained elevated panes, simple selected-row rails, keycap-style shortcut hints, structured list rows, and productized title/subtitle blocks. Avoid decorative rail/pill/card stacking.
- Gridix 2026-06-21 — Release-readiness audit ledger at `.claude/references/release-audit-2026-06-21.md`. 9 BLOCKERs + ~20 GAPs across logic chains / operation linkage / visual design. Headline root cause: no centralized derived-state invalidation cascade — every state-changing path (connect/disconnect/switch/DDL/grid-save) manually clears only some dependent state. Design one cascade before patching individual symptoms (the symptoms are its test matrix). Top blockers: grid edits not cleared after save (handler.rs:688), MySQL grid save wrong quoting (grid/mod.rs:155), unsaved edits dropped on tab close, no query-cancel UI, zombie manager.active on connect failure (database.rs:107), cross-connection ER corruption, hot-path panics in shortcut_tooltip.rs.
- User Preferences — (to be populated by user)
- Feedback Log — (to be populated during sessions)
